# =============================================================================
# Genuivo API Environment Variables
# =============================================================================
# Copy this file to .env and fill in your values.
# For local overrides, use .env.local (takes priority over .env)
#
# Priority order:
#   1. .env.local (highest - for local development overrides)
#   2. .env (standard environment file)
#   3. System environment variables
# =============================================================================

# -----------------------------------------------------------------------------
# Database (Required)
# -----------------------------------------------------------------------------
# PostgreSQL connection string
# Format: postgres://user:password@host:port/database
DATABASE_URL=postgres://user:password@localhost:5432/genuivo

# -----------------------------------------------------------------------------
# Firebase Admin SDK (Required)
# -----------------------------------------------------------------------------
# Your Firebase project ID
FIREBASE_PROJECT_ID=your-firebase-project-id

# Service account email from Firebase Console
FIREBASE_CLIENT_EMAIL=firebase-adminsdk@your-project.iam.gserviceaccount.com

# Private key from service account JSON (include the full key with newlines)
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# -----------------------------------------------------------------------------
# Site Admin Access (Optional)
# -----------------------------------------------------------------------------
# Comma-separated list of email addresses for site admin access.
# Site admins can access other users' data.
# Example: admin@example.com,another@example.com
SITEADMIN_EMAILS=

# -----------------------------------------------------------------------------
# ShapeShyft (Required for chat)
# -----------------------------------------------------------------------------
SHAPESHYFT_API_URL=https://api.shapeshyft.ai/api/v1/ai/ieoc8nmj/genui/genui
SHAPESHYFT_API_KEY=your-shapeshyft-api-key
SHAPESHYFT_TIMEOUT_MS=120000

# -----------------------------------------------------------------------------
# Server (Optional)
# -----------------------------------------------------------------------------
# Port to run the server on (default: 8022)
PORT=8022

# Environment mode: development, production, or test
NODE_ENV=development

# Bun environment mode (used for test detection)
BUN_ENV=
